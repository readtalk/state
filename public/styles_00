/* READTalk - Dark Mode (Aman, tidak mengganggu positioning) */

:root {
  --bg: #0a0a0a;
  --surface: #121212;
  --surface2: #1f1f1f;
  --text: #eeeeee;
  --text-secondary: #aaaaaa;
  --primary: #00f5ff;
  --bubble-me: #00f5ff;
  --bubble-other: #2a2a2a;
  --border: #333333;
}

/* Warna dasar */
html, body {
  background: var(--bg);
  color: var(--text);
}

/* Header Brand */
h4 {
  font-size: 1.95rem;
  font-weight: 700;
  letter-spacing: -0.7px;
}
h4 b {
  color: var(--primary);
}

/* Search Box */
#searchInput {
  background: var(--surface2) !important;
  border: 1px solid var(--border) !important;
  color: var(--text) !important;
  border-radius: 10px;
}

#searchInput:focus {
  border-color: var(--primary) !important;
}

/* Search Results */
#searchResults {
  background: var(--surface2);
  border: 1px solid var(--border);
  border-radius: 8px;
}

/* Bubble Chat */
.message .bubble {
  padding: 12px 17px;
  border-radius: 18px;
  line-height: 1.5;
}

.message.me .bubble {
  background: var(--bubble-me);
  color: #000;
}

.message.other .bubble {
  background: var(--bubble-other);
  color: var(--text);
}

.message .user {
  color: var(--text-secondary);
  font-size: 0.85rem;
}

/* Input Text Area & Tombol Send (hanya bentuk & warna) */
.send-message input,
.my-input-text {
  background: var(--surface2) !important;
  border: 1px solid var(--border) !important;
  color: var(--text) !important;
  border-radius: 9999px;     /* bentuk bulat modern */
}

.send-message input:focus,
.my-input-text:focus {
  border-color: var(--primary) !important;
  outline: none;
}

.send-message button {
  background: var(--primary) !important;
  color: #000 !important;
  border-radius: 50%;
  width: 54px;
  height: 54px;
  font-size: 1.75rem;
}

/* Placeholder */
.my-input-text::placeholder {
  color: #888 !important;
}

/* Modal */
dialog {
  background: var(--surface);
  color: var(--text);
  border: 1px solid var(--border);
  border-radius: 12px;
}
